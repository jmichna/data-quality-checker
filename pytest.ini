[pytest]
# Wyciszamy ostrzezenie pochodzace z wnetrza FastAPI/Starlette (TestClient + httpx).
# To nie jest nasz kod - nie mamy na nie wplywu, a zasmieca wynik testow.
filterwarnings =
    ignore:Using `httpx` with `starlette.testclient`:DeprecationWarning
